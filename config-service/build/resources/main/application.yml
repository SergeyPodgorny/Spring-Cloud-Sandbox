spring:
  cloud:
    config:

#        usage of a git repository
      server:
        git:
          uri: https://github.com/SergeyPodgorny/YML-Coinfigurations-storage
          skipSslValidation: true
#        usage of a local folder
#     server:
#        native:
#          search-locations: classpath:/services
#  profiles:
#    active: native
  security:
    user:
      password: admin

server:
  port: 8001




