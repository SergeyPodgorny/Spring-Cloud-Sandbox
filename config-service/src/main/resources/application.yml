spring:
  cloud:
    config:
      server:
        import: "optional:configserver:"
        native:
          search-locations: classpath:/services
  profiles:
    active: native
  security:
    user:
      password: admin

server:
  port: 8001




